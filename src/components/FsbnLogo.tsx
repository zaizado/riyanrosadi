import React from 'react';

interface FsbnLogoProps {
  className?: string;
  size?: number | string;
  showText?: boolean;
}

export const AppLogo: React.FC<FsbnLogoProps> = () => {
  return null;
};

export const FsbnLogo = AppLogo;
